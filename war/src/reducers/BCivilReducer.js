const initialState = {
    oreMines: {},
    oilFields: {},
    houses: {},
    airPowerPlant: {},
    hydroPowerPlant:{},
    infrastructure:{},
    oreWarehouse: {},
    oilWarehouse: {},
};

export default (state = initialState, action) => {
    switch (action.type) {
        case "ORE_MINES_UPDATE":

            break;
    }
}